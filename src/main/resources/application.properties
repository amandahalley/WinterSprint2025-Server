# ✅ Spring Boot runs on port 8080
server.port=8080

# ✅ MySQL Database Configuration (Make sure this is correct)
spring.datasource.url=jdbc:mysql://localhost:3306/aviation2025?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=keyin2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ✅ Hibernate (JPA) Settings (Ensures tables are created)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# ✅ Debugging (Optional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.main.allow-bean-definition-overriding=true
